// Use the `,` operator to create tuples, which are typed, fixed-size lists of values.
fn main => 1, 2, 3

// Check out the inferred return type of this function!
fn foo => (1, 2, 3), ('4', 5.0)

fn mixedTypes => 1, 2, '3'

// You can also create tuple type literals.
//
// In practice, though, it's more likely to wrap these in a typedef.
fn myTuple: (Num, Num) => 2, 2