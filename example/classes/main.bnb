class Point {
    x: Int
    y: Int

    new(this.x, this.y)

    fn add(other: Point) {
        x += other.x
        y += other.y
    }

    fn toString: String => '(%x, %y)'
}

fn main {
    var p1 = Point(5, 5)
    var p2 = Point(20, 20)
    p1.add(p2)
}
